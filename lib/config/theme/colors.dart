import 'package:flutter/material.dart';

// Clase en donde se agregaran los colores que se necesitaran
class AppColors {
  static const Color primaryColor = Color.fromRGBO(248, 151, 50, 1);
  static const Color sencudaryColor = Color.fromRGBO(227, 91, 93, 1);
}
